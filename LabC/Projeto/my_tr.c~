#include<stdio.h>
#include<string.h>


void my_tr(char* str1[], char* str2[]){

  char c;
  while(c = getchar() != EOF){
  for(int i=0, str[i]!='\0';i++){
    str1[i]=str2[i];
    printf("%c", str1[i]);
    }
 }
} 


int main(int argc, char* argv[]){

  my_tr(argv[1],argv[2]);

  return 0;
}
